<%= form_with(model: dummy_user) do |form| %>
  <% if dummy_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dummy_user.errors.count, "error") %> prohibited this dummy_user from being saved:</h2>

      <ul>
        <% dummy_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
